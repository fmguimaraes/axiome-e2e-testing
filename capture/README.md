# capture/

Placeholder for the deterministic Playwright capture harness (FR19, AXI-1381).
Not built in AXI-1369 — this story is the REST action→route audit and the
staging toolkit skeleton only. Kept as an empty, tracked directory so
`SI-044`'s registered path (`axiome-e2e-testing/capture/**`) exists from the
first story, and so `staging/checks/noBackdoor.ts` has something to scan here
once code lands.
